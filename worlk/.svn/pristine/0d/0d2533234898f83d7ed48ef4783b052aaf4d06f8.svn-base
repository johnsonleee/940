package com.vinbet.mobile.test;

import java.io.File;
import java.text.DecimalFormat;
import java.text.ParseException;
import java.util.ArrayList;
import java.util.List;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class Test {
	private static final Logger logger = LogManager.getLogger(Test.class);

	public static void main(String[] args) throws ParseException {
		// String all = "";
		// int index = 0;
		// try {
		// BufferedReader br = new BufferedReader(new FileReader(new
		// File("D:\\domain.txt" )));
		// String str = null;
		// while ((str = br.readLine()) != null) {
		// // vinbet{manifestPlaceholders=[APP_NAME_VALUE: " vinbet"]}
		// String name = str.toLowerCase().toString().trim();
		// all += name.substring(0, name.indexOf(".")) +
		// "{manifestPlaceholders=[APP_NAME_VALUE:\"" + name + "\"]}\n";
		// }
		// System. out.println(all);
		// } catch (Exception e) {
		// e.printStackTrace();
		// }
		//

		//File file = new File("C:/Users/Administrator/Desktop/APK");
		//File[] files = file.listFiles(); // 得到f文件夹下面的所有文件。
		//for (File f : files) {
		//	System.out.println(f.getName());
		//}
		
		
		DecimalFormat df1 = new DecimalFormat("##.00");  
		 System.out.println(df1.format(0.00));
		 
		 List list = new ArrayList();
		 System.out.println(list.get(0));
		
		
	}

	public void test() {
		System.out.println(this);
	}
}
